import { describe, it } from "vitest";

describe.skip("anvil integration scaffold", () => {
  it("will cover deposit, approve, market reads, swap estimate, order placement, cancel, packed replace, and signer authorization", () => {
    // Intentionally skipped until the contracts repo has a stable local deploy script for the spot stack.
  });
});
